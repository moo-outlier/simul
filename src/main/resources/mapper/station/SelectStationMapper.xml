<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cbm.station.dao.SelectStationDAO">

  <select id="getStationList" resultType="cmmcloud.data.CamelLinkedHashMap">
-- getStationList
-- 역 목록 조회
    SELECT
      LINE_NO,
      STATION_NO,
      STATION_NM,
      R_STATION_NO,
      R_STATION2_NO,
      L_STATION_NO
    FROM MN_STATION_INFO
    WHERE USE_AT = 'Y'
    ORDER BY STATION_INFO_PID ASC
  </select>

</mapper>
