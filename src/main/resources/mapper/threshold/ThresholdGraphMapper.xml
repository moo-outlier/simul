<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cbm.threshold.dao.ThresholdGraphDAO">

  <insert id="createThresholdGraphDummy">
--  createThresholdGraphDummy
--  더미데이터 생성용 - 임계치 진단 결과 그래프 목록 조회
    INSERT INTO MN_THRHLD_RESULT_GRAPH
    (
      PV_UNQCODE,
      DEVICE_UNQNO,
      DEVICE_CODE,
      CAR_SE_CODE,
      DEVICE_NM,
      WARN_RT,
      STRANGE_RT,
      OCRNC_AT,
      CREATOR,
      UPDATER
    )
    VALUES
    <foreach collection="list" item="item" separator=",">
    (
      #{item.pvUnqcode},
      #{item.deviceUnqno},
      #{item.deviceCode},
      #{item.carSeCode},
      #{item.deviceNm},
      #{item.warnRt},
      #{item.strangeRt},
      #{item.ocrncAt},
      'system',
      'system'
    )
    </foreach>
  </insert>

</mapper>